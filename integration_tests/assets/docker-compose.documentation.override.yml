version: '3.7'
services:
  sync:
    depends_on:
      - auth
      - chatd
      - postgres
      - swagger-validator
    environment:
      TARGETS: "auth:9497 chatd:9304 swagger-validator:8080 postgres:5432"

  swagger-validator:
    image: swaggerapi/swagger-validator
    ports:
      - '8080'
